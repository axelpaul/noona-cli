{
	"name": "noona-cli",
	"version": "0.1.0",
	"description": "CLI for the Noona booking marketplace — search businesses, list services, find open time slots, and book/cancel appointments from your terminal. AI-agent friendly. Built with Bun.",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"bin": { "noona": "src/index.ts" },
	"scripts": {
		"start": "bun src/index.ts",
		"dev": "bun --watch src/index.ts",
		"build": "bun build --compile src/index.ts --outfile noona",
		"typecheck": "bunx tsc --noEmit",
		"lint": "bunx biome check src/",
		"check": "bun run lint && bun run typecheck"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"@types/bun": "latest",
		"typescript": "^5.6.0"
	}
}
